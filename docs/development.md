# Development

## Commands

```bash
cargo build
cargo test
cargo fmt
```

## Project Rules

- Keep the project conversion-only.
- Store built-in visual presets as JSON theme files.
- Add tests in the touched module for parser, terminal, theme, or renderer changes.
- Update docs when the CLI surface or theme schema changes.
